# Planet Pipeline - Requirements
# v2.0.0

# Core (required)
requests>=2.28.0
numpy>=1.24.0
rasterio>=1.3.0
shapely>=2.0.0
tqdm>=4.65.0

# ML frameworks (optional - install what you need)
# pip install torch          # PyTorch
# pip install tensorflow     # TensorFlow
# pip install scikit-learn   # DBSCAN/HDBSCAN clustering

# Additional geospatial (optional)
# pip install geopandas      # Advanced vector operations
# pip install pyproj         # Coordinate transformations

# Visualization (optional)
# pip install Pillow         # Mask PNG export

# Development (optional)
# pip install pytest pytest-cov black flake8 mypy
