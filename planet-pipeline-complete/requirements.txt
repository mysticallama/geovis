# Planet Labs Imagery Processing Pipeline Requirements

# Core dependencies
requests>=2.28.0
numpy>=1.24.0
rasterio>=1.3.0
tqdm>=4.65.0

# Optional but recommended
python-dotenv>=1.0.0  # Environment variable management

# For ML preparation (install based on your framework)
torch>=2.0.0  # PyTorch (optional)
tensorflow>=2.12.0  # TensorFlow (optional)
scikit-learn>=1.3.0  # scikit-learn (optional)

# For geospatial operations
shapely>=2.0.0
geopandas>=0.13.0  # Optional for advanced geometry operations
pyproj>=3.5.0

# Development and testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.4.0
