# ─────────────────────────────────────────────────────────────────────────────
# Water & Food Infrastructure Monitoring Pipeline — API Credentials
#
# Copy this file to .env and fill in your keys:
#   cp .env.example .env
#
# .env is listed in .gitignore and will NEVER be committed to version control.
# ─────────────────────────────────────────────────────────────────────────────

# ── ACLED (Armed Conflict Location & Event Data) ──────────────────────────────
# Required for Step 2 (conflict event queries).
# Register at: https://acleddata.com/register/
# OAuth tokens are fetched automatically from email + password.
ACLED_EMAIL=acled.email
ACLED_PASSWORD=acled.password

# Optional: pre-obtained access token (valid 24 h). Overrides email+password.
# ACLED_ACCESS_TOKEN=

# ── Planet Labs ───────────────────────────────────────────────────────────────
# Required for Step 4 (imagery download).
# Get a key at: https://www.planet.com/
PL_API_KEY=planetapikey

# ── Mapbox ────────────────────────────────────────────────────────────────────
# Required for Step 7 (interactive web app).
# Get a public token at: https://account.mapbox.com/access-tokens/
# Public tokens start with "pk."
MAPBOX_TOKEN=pk.apikey
# ── HDX / Humanitarian Data Exchange (optional) ───────────────────────────────
# Used in Step 6 (WFP logistics data).
# Most WFP datasets on HDX are publicly accessible without a key.
# An API key allows access to restricted organisation datasets.
# Get a key at: https://data.humdata.org/user/register
# HDX_API_KEY=
